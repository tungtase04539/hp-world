assets/*.glb
# Bộ asset nhẹ cho máy yếu — SINH TỰ ĐỘNG (bash tools/make_lite_assets.sh),
# nặng 67MB nên đẩy sang nhánh assets-storage như assets/ gốc, không commit ở nhánh làm việc.
assets_lite/*.glb
glbview.html
/rb
node_modules
water_debug.png

# OSM data tải từ Overpass (regeneratable: bash tools/fetch_osm.sh) — nặng, không commit
tools/osm_*.json
tools/mask_debug.png
